export class YarnLength {
	color: string;
	count: number;
	yarnLength: number;
	
    constructor(color: string, count: number, yarnLength: number) {
        this.color = color;
		this.count = count;
		this.yarnLength = yarnLength;
    }
}
